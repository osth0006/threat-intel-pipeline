services:
  pipeline:
    build: .
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
      - N_REPORTS=100
      - CLASSIFIER_TYPE=tfidf_sgd
